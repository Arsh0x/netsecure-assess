import {useEffect,useState} from 'react'
import {Laptop,Network,Search,Server} from 'lucide-react'
import {Link,useSearchParams} from 'react-router-dom'
import {api} from '../lib/api'
import {Empty,PageHeader,RiskBadge,Spinner,Status} from '../components/ui'

export default function Assets(){const [params]=useSearchParams();const [items,setItems]=useState<any[]|null>(null);const [search,setSearch]=useState('');useEffect(()=>{api.get<any[]>(`/assets${params.get('project')?`?project_id=${params.get('project')}`:''}`).then(setItems)},[params]);if(!items)return <Spinner/>;const shown=items.filter(a=>(a.hostname||'').toLowerCase().includes(search.toLowerCase())||a.ip_address.includes(search));return <><PageHeader eyebrow="Inventory" title="Assets" description="Devices discovered within documented, authorized project scopes."/><div className="toolbar"><div className="search-box"><Search/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search hostname or address"/></div><button className="secondary">All operating systems</button><button className="secondary">All risk levels</button></div>{shown.length===0?<Empty title="No matching assets" body="Run a demo scan or adjust your filters."/>:<div className="table-panel"><table><thead><tr><th>Asset</th><th>Status</th><th>Probable OS</th><th>Open ports</th><th>Criticality</th><th>Risk</th><th>Last seen</th></tr></thead><tbody>{shown.map(a=><tr key={a.id}><td><Link className="asset-cell" to={`/assets/${a.id}`}><span>{a.probable_os.includes('Windows')?<Laptop/>:<Server/>}</span><div><b>{a.hostname||'Unresolved host'}</b><small>{a.ip_address}</small></div></Link></td><td><Status value={a.status}/></td><td>{a.probable_os}</td><td><span className="port-count"><Network/>{a.open_ports}</span></td><td>{'●'.repeat(a.criticality)}<span className="muted-dots">{'●'.repeat(5-a.criticality)}</span></td><td><RiskBadge score={a.risk_score}/></td><td>{new Date(a.last_seen).toLocaleDateString()}</td></tr>)}</tbody></table></div>}</>}

